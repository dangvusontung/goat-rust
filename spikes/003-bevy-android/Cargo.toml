[package]
name = "bevy_android_poc"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bevy = { version = "0.19.0", features = ["android-native-activity"] }

[target.'cfg(target_os = "android")'.dependencies]
bevy_android = "0.19.0"

[workspace]

[profile.dev.package."*"]
opt-level = 3
